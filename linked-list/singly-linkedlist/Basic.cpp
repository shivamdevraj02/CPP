NOTE :--

👉 Linked List, Stack (DS), Queue, Tree jaise data structures heap me isliye bante hain kyunki:

Size runtime pe decide hota hai

Lifetime function se bahar chahiye hoti hai

Stack memory limited hoti hai

Dynamic grow / shrink chahiye hota hai



Linked List :--  Linear Data structure which is basically a collection of nodes (nodes store data and the next node address ) .

-> it is a dynamic data structure , means it can be grow and shrink at the run time .

-> so we can say their is no wastage of memory .

-> we can easily insert and delete element in it.


In C++, a linked list is a linear data structure that allows the users to store data in non-contiguous memory locations. A linked list is defined as a collection of nodes where each node consists of two members which represents its value and a next/previous pointer which stores the address for the next/previous node


* why linked list is needed ?

1) Dynamic Size:

2) Efficient Insertions and Deletions: 

3) No Memory Wastage:

4) Non-Contiguous Storage:


* Types of linked list :--

1) Singly LL 

2) Doubly LL 

3) Circular LL 

4) Circular doubly LL 








